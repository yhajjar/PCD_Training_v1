# ===========================================
# PCD Training App - Environment Variables
# ===========================================

# PocketBase base URL (ORIGIN ONLY, no "/api" suffix)
# Frontend requests always target "/api/..." under this origin.
# Do NOT use route-relative values (e.g. "", ".", "api") in deployed env files.
# Recommended production value: https://training-hub.ku.ac.ae
# Development value (direct PB): http://127.0.0.1:8090
# If omitted/empty, runtime falls back to window.location.origin.
VITE_POCKETBASE_URL=https://training-hub.ku.ac.ae

# === Auto-Provisioning ===
# SSO users are provisioned/authenticated by backend /whoami.
# Frontend should never receive or store PocketBase admin/superuser tokens.
# Admins must be assigned manually via Team Roles tab in admin portal.

# ===========================================
# Deployment Architecture (Coolify)
# ===========================================
# 
# Service 1: PocketBase
# - Image: ghcr.io/pocketbase/pocketbase:latest
# - Port: 8090
# - Requires GitHub Container Registry credentials in Coolify settings
#
# Service 2: PCD App
# - Built from Dockerfile
# - Port: 8022
# - Build arg: VITE_POCKETBASE_URL points to PocketBase service
#
# Services are deployed separately on Coolify.
# ===========================================
